<?php
	if($var == true){	}
	if($var)	{	}
	
	$idx = substr($haystack, $needle);
	if($idx === false){	// (works with !== for the opposite effect)
		//logic when $needle not found in $haystack
	}
	else
	{
		// logic when $needle    found in $haystack
	}
?>
