#include <iostream>

const int SECRET_NUM = 11,213;
const PAY_RATE = 18.35;

int fonction1(){
  int one, two; // one et two sont des entiers
  double first, second; // first et second sont des float
  one = 18;
  two = 11; // on leur donne une valeur

  first = 25;
  second - first * three;

  second = 2 * SECRET_NUM;
  SECRET_NUM = SECRET_NUM + 3;
  cout << first << " " << second << SECRET_NUM << endl;

  paycheck = hoursWorked * PAY_RATE;
  cout << "Wages = " << paycheck << endl;
  return 0;
}


const char = STAR = `*`;
const int = PRIME = 71;

int fonction2(){
  int count, sum;
  double x;

  count = 1;
  sum = count + PRIME;
  x := 25.67;

  newNum = count * ONE + 2;
  sum + count = sum;
  x = x + sum * COUNT;

  cout << "count = " << count << ", sum = " << sum
       << ", PRIME = " << Prime << endl;
  return 0;
}
